<body>
	<% if flash[:notice] %>
		<div class="flash">
			<%= flash[:notice] %>
		</div>
	<% end %>

	<p class="show-p"><strong>Title: </strong><%= @book.title %></p>
	
	<p class="show-p"><strong>Body: </strong><%= @book.body %></p>

	<p><%= link_to 'Edit', edit_book_path(@book.id) %> | <%= link_to 'Back', '/books' %></p>
</body>